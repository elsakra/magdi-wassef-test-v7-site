---
import BaseLayout from '@/layouts/BaseLayout.astro';
import Card from '@/components/Card.astro';
import siteData from '@/data/site.json';
import { getCollection } from 'astro:content';

const posts = await getCollection('blog');
const sortedPosts = posts.sort((a, b) => 
  new Date(b.data.publishedAt).getTime() - new Date(a.data.publishedAt).getTime()
);
---

<BaseLayout title="Blog">
  <!-- Hero -->
  <section class="pt-32 pb-16 bg-gradient-to-b from-sage-50 to-cream-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="max-w-3xl mx-auto text-center">
        <h1 class="font-display text-4xl sm:text-5xl lg:text-6xl font-semibold text-sage-900">
          Health & Wellness Insights
        </h1>
        <p class="mt-6 text-xl text-earth-600 leading-relaxed">
          Expert advice, tips, and stories to support your health journey.
        </p>
      </div>
    </div>
  </section>

  <!-- Blog Grid -->
  <section class="py-24 bg-cream-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      {sortedPosts.length > 0 ? (
        <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
          {sortedPosts.map((post) => (
            <article class="bg-white rounded-2xl overflow-hidden shadow-sm hover:shadow-lg transition-shadow">
              {post.data.image && (
                <a href={`/blog/${post.slug}`} class="block aspect-[16/9] overflow-hidden">
                  <img 
                    src={post.data.image} 
                    alt={post.data.title}
                    class="w-full h-full object-cover hover:scale-105 transition-transform duration-500"
                  />
                </a>
              )}
              <div class="p-6">
                <div class="flex items-center gap-4 text-sm text-earth-500 mb-3">
                  <time datetime={post.data.publishedAt}>
                    {new Date(post.data.publishedAt).toLocaleDateString('en-US', {
                      year: 'numeric',
                      month: 'long',
                      day: 'numeric'
                    })}
                  </time>
                  {post.data.category && (
                    <>
                      <span>•</span>
                      <span class="text-sage-600">{post.data.category}</span>
                    </>
                  )}
                </div>
                <h2 class="font-display text-xl font-semibold text-sage-900">
                  <a href={`/blog/${post.slug}`} class="hover:text-sage-700 transition-colors">
                    {post.data.title}
                  </a>
                </h2>
                <p class="mt-2 text-earth-600 text-sm leading-relaxed line-clamp-3">
                  {post.data.excerpt}
                </p>
                <a 
                  href={`/blog/${post.slug}`}
                  class="mt-4 inline-flex items-center text-sage-600 font-medium text-sm hover:text-sage-800 transition-colors"
                >
                  Read More
                  <svg class="ml-1 w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
                  </svg>
                </a>
              </div>
            </article>
          ))}
        </div>
      ) : (
        <div class="text-center py-12">
          <p class="text-earth-600 text-lg">No blog posts yet. Check back soon!</p>
        </div>
      )}
    </div>
  </section>
</BaseLayout>
