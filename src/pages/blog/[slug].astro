---
import BaseLayout from '@/layouts/BaseLayout.astro';
import Button from '@/components/Button.astro';
import { getCollection } from 'astro:content';

export async function getStaticPaths() {
  const posts = await getCollection('blog');
  return posts.map((post) => ({
    params: { slug: post.slug },
    props: { post },
  }));
}

const { post } = Astro.props;
const { Content } = await post.render();
---

<BaseLayout title={post.data.title} description={post.data.excerpt}>
  <article>
    <!-- Header -->
    <header class="pt-32 pb-16 bg-gradient-to-b from-sage-50 to-cream-50">
      <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center gap-4 text-sm text-earth-500 mb-6">
          <time datetime={post.data.publishedAt}>
            {new Date(post.data.publishedAt).toLocaleDateString('en-US', {
              year: 'numeric',
              month: 'long',
              day: 'numeric'
            })}
          </time>
          {post.data.category && (
            <>
              <span>•</span>
              <span class="text-sage-600">{post.data.category}</span>
            </>
          )}
          {post.data.author && (
            <>
              <span>•</span>
              <span>By {post.data.author}</span>
            </>
          )}
        </div>
        
        <h1 class="font-display text-3xl sm:text-4xl lg:text-5xl font-semibold text-sage-900 leading-tight">
          {post.data.title}
        </h1>
        
        <p class="mt-6 text-xl text-earth-600 leading-relaxed">
          {post.data.excerpt}
        </p>
        
        {post.data.tags && post.data.tags.length > 0 && (
          <div class="mt-6 flex flex-wrap gap-2">
            {post.data.tags.map((tag) => (
              <span class="px-3 py-1 bg-sage-100 text-sage-700 text-sm rounded-full">
                {tag}
              </span>
            ))}
          </div>
        )}
      </div>
    </header>

    <!-- Featured Image -->
    {post.data.image && (
      <div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 -mt-8">
        <div class="aspect-[2/1] rounded-2xl overflow-hidden shadow-xl">
          <img 
            src={post.data.image} 
            alt={post.data.title}
            class="w-full h-full object-cover"
          />
        </div>
      </div>
    )}

    <!-- Content -->
    <div class="py-16 bg-cream-50">
      <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="prose prose-lg prose-sage prose-headings:font-display prose-headings:text-sage-900 prose-a:text-sage-600 prose-a:no-underline hover:prose-a:underline max-w-none">
          <Content />
        </div>
      </div>
    </div>

    <!-- CTA -->
    <section class="py-16 bg-white">
      <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h2 class="font-display text-2xl font-semibold text-sage-900">
          Ready to take the next step in your health journey?
        </h2>
        <p class="mt-3 text-earth-600">
          Book a consultation to discuss your health goals.
        </p>
        <div class="mt-6 flex justify-center gap-4">
          <Button href="/contact" style="primary">
            Book a Consultation
          </Button>
          <Button href="/blog" style="outline">
            More Articles
          </Button>
        </div>
      </div>
    </section>
  </article>
</BaseLayout>
